workflows:
  # ── iOS Build (App Store) ──
  build-ios:
    name: Build iOS (ZZmore Store)
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      vars:
        BUNDLE_ID: store.zzmore.app
    scripts:
      - name: Install dependencies
        script: flutter pub get
      - name: Set version from pubspec
        script: |
          BUILD_NUMBER=$(grep 'version:' pubspec.yaml | head -1 | awk '{print $2}' | cut -d '+' -f2)
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion $BUILD_NUMBER" ios/Runner/Info.plist
      - name: Set up code signing
        script: |
          if [ -n "$APP_STORE_CONNECT_PRIVATE_KEY" ]; then
            echo "API key found — configuring App Store code signing"
            keychain initialize
            app-store-connect fetch-signing-files "$BUNDLE_ID" \
              --type IOS_APP_STORE \
              --create
          else
            echo "No API key set — building unsigned IPA (you must sign it manually)"
          fi
      - name: Build iOS IPA
        script: |
          if [ -n "$APP_STORE_CONNECT_PRIVATE_KEY" ]; then
            flutter build ipa --release \
              --export-options-plist=$HOME/export_options.plist
          else
            flutter build ipa --release --no-codesign
          fi
      - name: Verify and rename IPA
        script: |
          echo "=== Contents of build/ios/ipa/ ==="
          ls -la build/ios/ipa/ 2>/dev/null || echo "No ipa/ directory yet"
          echo "=== Searching for IPA anywhere in build/ ==="
          find build/ -name "*.ipa" 2>/dev/null | head -10
          echo "=== Searching for .app ==="
          find build/ -name "*.app" -type d 2>/dev/null | head -5
          IPA_PATH=$(find build/ -name "*.ipa" | head -1)
          if [ -n "$IPA_PATH" ]; then
            mv "$IPA_PATH" build/ios/ipa/Zzmorestore.ipa
            echo "IPA ready: build/ios/ipa/Zzmorestore.ipa"
          else
            echo "No IPA found. Listing all files in build/ios/:"
            find build/ios -type f 2>/dev/null | head -30
            exit 1
          fi
    artifacts:
      - build/ios/ipa/Zzmorestore.ipa
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        key_id: $APP_STORE_CONNECT_KEY_ID
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        submit_to_testflight: false
        submit_to_app_store: false

  # ── Android Build ──
  build-android:
    name: Build Android (ZZmore Store)
    environment:
      flutter: stable
    scripts:
      - name: Install dependencies
        script: flutter pub get
      - name: Build Android APK
        script: flutter build apk --release
      - name: Rename APK
        script: mv build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/Zzmorestore.apk
    artifacts:
      - build/app/outputs/flutter-apk/Zzmorestore.apk
